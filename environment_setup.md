# 环境搭建与协作配置

本项目先后使用了两套计算环境：海南试验区的公用电脑，以及后来个人自建的深度学习服务器。以下记录具体配置与遇到的问题。

## 1. 试验区公用电脑

- **地点**：海南陵水黎安国际教育创新试验区实验楼 424 教室。
- **硬件**：搭载一张 NVIDIA RTX 4090 的台式机。
- **操作系统**：重装为 Ubuntu 25.10。
- **远程访问**：
  - 开启 SSH 服务，可在园区内网中远程登录，无法从外网直接连接。
  - 尝试申请端口转发，后被拒绝。
  - 内网穿透方案被相关人员警告，未继续推进。
- **其他配置**：
  - 已开启远程唤醒（Wake-on-LAN）。
  - 从 NO-IP 申请动态域名，通过 Docker 配置脚本自动更新 IP。
- **服务中断**：2026 年 1 月 23 日接到通知，424 房间假期停用并断电，该环境停止使用。

## 2. conda 包管理器问题（Ubuntu 25.10）

在试验区电脑的 Ubuntu 25.10 系统上，无法安装任何版本的 Miniconda 或 Anaconda，多种安装方式均失败。后来在 Anaconda 官方论坛（forum.anaconda.com）找到原因：Ubuntu 25.10 的 core-uutils 包含新的 Rust 组件，导致安装程序出错。

**解决方法**（在终端执行）：
```bash
sudo apt install coreutils-from-gnu coreutils-from-uutils- --allow-remove-essential
```
执行后，2025.06 版本的安装程序可正常运行。

在此问题解决前，团队改用 micromamba 管理虚拟环境。micromamba 安装在 `/local/share` 下，理论上多用户可共用。即使后续成功安装了 Miniconda，项目仍沿用 micromamba 管理环境。

## 3. 自建服务器

### 3.1 硬件配置
- CPU：R7 9700X（散片）
- 主板：B850 TOMAHAWK WiFi 战斧导弹
- 机箱/电源：联立 L217 风道机箱 + 刃界联力 1200W 金牌全模组
- 散热：库里奥倚天 P60T 性能版-B [V2]
- 存储：宏碁掠夺者 GM9 2TB NVMe SSD
- 内存：镁光 DDR5 32GB 5200MHz（双面颗粒）
- 显卡：初始选用 GeForce RTX 5090 D 24G 技嘉魔鹰，后因 CUDA 兼容性问题更换为影驰 GeForce RTX 3090 GAMER OC

### 3.2 操作系统
- 首次安装 Ubuntu 22.04 LTS，出现以下问题：
  - 安装完成后无法进入 BIOS 设置。
  - 系统无法识别有线及无线网卡。
- 重装 Ubuntu 24.04.5 LTS 后问题解决，所有硬件正常工作。

### 3.3 远程访问
- 家庭宽带运营商为中国移动，无 IPv4 公网地址，有 IPv6 公网地址。
- IPv6 在内网可连通，但从外网无法连接，即使关闭防火墙或修改其他设置仍不通。
- 光猫重启后 IPv6 前缀会变化，进一步增加直连难度。
- **最终方案**：采用 ZeroTier 构建虚拟局域网，为服务器分配固定虚拟 IP。所有项目成员安装 ZeroTier 客户端并加入同一网络后，可通过该虚拟 IP 稳定连接至服务器。

### 3.4 权限管理
- 创建用户组 `3d_team`，所有项目成员均以普通用户身份加入。
- SSH 配置：
  - 具有 sudo 权限的账户仅允许通过密钥登录，禁止密码登录。
  - root 用户禁止远程登录。
- 共享目录 `/data`、`/opt/miniconda3` 等设置了 setgid 位，确保组内成员创建的文件所属组统一，权限可控。

### 3.5 项目环境
- 源码克隆至 `/data/workspace`，多个项目仓库。
- 使用 conda 搭建独立虚拟环境。
- MiniGPT-3D 所需的所有数据文件、权重文件均已下载并放置于约定路径。
- 启动了 Gradio 网页服务，团队成员通过 SSH 本地端口转发在自己电脑的浏览器中访问，用于初步调试 3D 点云理解效果。

